use super::*;
use crate::{container::Container, Up};

#[tokio::test]
async fn construct_up() {
    //let root = Up::<Mother>::create(UserData, Key).await.unwrap();
    //println!("{:#?}", std::any::type_name_of_val(&root));
    //println!("{:#?}", root);
}
